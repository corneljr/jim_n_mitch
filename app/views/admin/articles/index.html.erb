<h3>Articles</h3>
<ul class='admin-article-list'>
	<% @articles.each do |article| %>
		<li><%= "#{article.title} (#{article.author})" %> <%= link_to 'Edit', edit_admin_article_path(article) %> <%= link_to 'Delete', admin_article_path(article), method: 'delete' %></li>
	<% end %>
</ul>